# My No Good Very Bad Project

Welcome, take a seat and have a look around. We have a pool and refreshments...

```{contents}
```

```{math}
J_\alpha(x) = \frac{\left(\frac{x}{2}\right)^\alpha}{\Gamma(\alpha+1)} \;_0F_1 \left(-;\alpha+1; -\frac{x^2}{4}\right)
```

```{literalinclude} ../fast_inverse_sqrt.c
```
